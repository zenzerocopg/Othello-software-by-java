package othello.software.javaproject;

enum TableStatus {
	
	NORMAL, SELECT, ACTIVE, FINAL, MARGIN
	
	// NORMAL = blank
	// SELECT = if it  have dot 
	// ACTIVE = if it's  summit
	// final = if we have dot
	// MARGIN = margin

};
