package othello.software.javaproject;

enum TableStatus {
	
	NORMAL, SELECT, ACTIVE, FINAL, MARGIN
	// NORMAL = blank
	// SELECT = if it can have dot 
	// ACTIVE = if it's can summit
	// final = if we have dot
	// MARGIN = margin
	
};
